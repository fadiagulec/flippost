# Local Netlify folder
.netlify

# Real secrets. `.env.example` IS tracked — it documents what to set and why,
# with no real values. Anything that could hold a live key stays out.
.env
.env.*
!.env.example

# Node modules
node_modules/

# Claude Code workspace metadata
.claude/

# Chrome extension build artifacts (regenerable; not source)
flipit-extension-*.zip

# Local audit scratch (not source-of-truth)
AUDIT.md

# NOTE: package.json and package-lock.json ARE tracked now — they declare
# @netlify/blobs and @netlify/functions at the project root so Netlify's
# `npm install` actually pulls them in. Without these, the rate limiter
# fail-opens in production (see _rate_limit.js loadBlobs() catch path).

# Chrome Web Store submission scratch (regenerated locally)
submission-assets/

